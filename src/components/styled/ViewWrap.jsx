import styled from 'styled-components';

const ViewWrap = styled.main`
  max-width: 1200px;
  position: relative;
  padding: 1rem;
  font-size: 1.4rem;
  margin: auto;
  overflow: auto;
`;

export default ViewWrap;
